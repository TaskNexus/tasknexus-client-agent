[package]
name = "tasknexus-updater"
version = "0.1.0"
edition = "2021"
description = "TaskNexus Agent updater helper"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
